name: taxifundriver
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

    # Ajout du package partagé par chemin relatif
  taxifun_core:
    path: ../taxifun_core

  cupertino_icons: ^1.0.8
  pin_code_fields: ^8.0.1   # optionnel – très beau pour OTP

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Ajout des assets
  assets:
    - assets/truck.png
    - assets/flags/CM.png