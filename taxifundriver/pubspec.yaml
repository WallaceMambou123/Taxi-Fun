name: taxifundriver
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  pin_code_fields: ^8.0.1   # optionnel – très beau pour OTP
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  fluttertoast: ^8.2.8  # Pour messages sympas

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Ajout des assets
  assets:
    - assets/truck.png
    - assets/flags/CM.png